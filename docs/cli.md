# CLI Reference

The full `bioamla` command tree, generated from the CLI itself (`--help`), so it
always matches the installed version.

::: mkdocs-click
    :module: bioamla.cli.cli
    :command: cli
    :prog_name: bioamla
    :depth: 1
