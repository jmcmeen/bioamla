"""Adapters for external library integrations.

This package contains adapters that wrap external libraries (like OpenSoundscape)
to provide bioamla-compatible interfaces. Only the services layer should import
from these adapters - core code should not depend on them.
"""
