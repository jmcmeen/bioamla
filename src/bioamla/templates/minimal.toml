# Bioamla Project Configuration - Minimal Template
# Generated by: bioamla project init
#
# A minimal configuration for quick tasks. Only essential settings are
# included here - all other settings use bioamla defaults.
#
# To see all available settings, run: bioamla config show --defaults
# Or use the "default" template: bioamla project init --template default

# =============================================================================
# PROJECT METADATA
# Basic information about this project
# =============================================================================
[project]
name = ""
version = "1.0.0"
description = ""
created = ""

# Add additional settings as needed. Common sections:
#
# [audio]          - Audio preprocessing (sample_rate, mono, normalize)
# [visualize]      - Spectrogram settings (type, n_fft, hop_length, n_mels)
# [models]         - Model configuration (default_model, cache_dir)
# [inference]      - Prediction settings (batch_size, top_k, min_confidence)
# [training]       - Training settings (learning_rate, epochs, batch_size)
# [output]         - Output format (format, verbose, overwrite)
# [logging]        - Logging settings (level, max_history)
